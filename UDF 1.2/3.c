#include<stdio.h>

main()
{
	int i,len,upr=0,lwr=0,num=0,spe=0;

char password[10];
char email[20];

printf("Enter any string: ");
gets(email);


len = strlen(email);

printf("The length of string is %d",len);
}
